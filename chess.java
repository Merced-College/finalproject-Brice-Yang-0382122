/* Brice Yang

*/

public class chess{
    public static void main(String[] args){
        System.out.println("White goes first.");

        /* EXAMPLE 
        while(king not checkmated){


            whiteMove(); asks for position input. Then checks if it's valid.
            ||
            updateBoard(); visually updates the ASCII board
            ||
            allChessChecks(); checks/captures/checkmates/stalemates


            blackMove(); asks for position input. Then checks if it's valid.
            ||
            updateBoard(); visually updates the ASCII board
            ||
            allChessChecks(); checks/captures/checkmates/stalemates.

            
        }
        */
    }
}
